#Create network
docker network create -d bridge honeypot-network

#Docker build with Dockerfile
docker build -t heralding .

#Docker run
docker run -it --network=honeypot-network heralding --name heralding
